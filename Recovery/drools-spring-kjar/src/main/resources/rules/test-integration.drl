package sbnz.integracija;

import com.sbnz.recovery.model.Patient;


rule "Patient existance"
    when
        $p: Patient( )
    then
    	$p.setBmi(20.0);
    	update($p);
        System.out.println("Patient exists in memory");
end